---
title: thv plugin install
hide_title: true
description: Reference for ToolHive CLI command `thv plugin install`
last_update:
  author: autogenerated
slug: thv_plugin_install
mdx:
  format: md
---

## thv plugin install

Install a plugin

### Synopsis

Install a plugin by name or OCI reference.
The plugin will be fetched from a remote registry and installed locally.

```
thv plugin install [plugin-name] [flags]
```

### Options

```
      --clients string        Comma-separated target client apps (e.g. claude-code,opencode), or "all" for every available client
      --force                 Overwrite existing plugin directory
      --group string          Group to add the plugin to after installation
  -h, --help                  help for install
      --project-root string   Project root path for project-scoped installs
      --scope string          Installation scope (user, project) (default "user")
```

### Options inherited from parent commands

```
      --debug   Enable debug mode
```

### SEE ALSO

* [thv plugin](thv_plugin.md)	 - Manage plugins

