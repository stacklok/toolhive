apiVersion: v1
kind: Service
metadata:
  name: mcp-st-streamable-http
  namespace: toolhive-system
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 1800
