apiVersion: v1
kind: Pod
metadata:
  labels:
    app: mcpserver
    app.kubernetes.io/instance: yardstick
  namespace: toolhive-system
spec:
  containers:
  - name: toolhive
    # Container should exist - image will be proxy runner, not yardstick server
status:
  phase: Running