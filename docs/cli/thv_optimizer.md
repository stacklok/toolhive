---
title: thv optimizer
hide_title: true
description: Reference for ToolHive CLI command `thv optimizer`
last_update:
  author: autogenerated
slug: thv_optimizer
mdx:
  format: md
---

## thv optimizer

Semantic tool discovery and ingestion for MCP servers

### Synopsis

The optimizer command provides semantic tool discovery by ingesting MCP workloads,
generating embeddings for tools, and enabling semantic search capabilities.

This is a standalone command for testing the optimizer functionality. In production,
the optimizer can be integrated into the vMCP process as a goroutine.

### Options

```
      --db-path string        Path to SQLite database (default: ~/.toolhive/optimizer.db)
  -h, --help                  help for optimizer
      --model-path string     Path to ONNX embedding model (required)
      --runtime-mode string   Runtime mode: docker or k8s (default "docker")
```

### Options inherited from parent commands

```
      --debug   Enable debug mode
```

### SEE ALSO

* [thv](thv.md)	 - ToolHive (thv) is a lightweight, secure, and fast manager for MCP servers
* [thv optimizer ingest](thv_optimizer_ingest.md)	 - Ingest MCP workloads and generate embeddings
* [thv optimizer query](thv_optimizer_query.md)	 - Query tools using semantic search
* [thv optimizer status](thv_optimizer_status.md)	 - Show optimizer status and statistics

