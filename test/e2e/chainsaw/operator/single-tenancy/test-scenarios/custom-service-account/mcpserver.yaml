apiVersion: toolhive.stacklok.dev/v1alpha1
kind: MCPServer
metadata:
  name: ($testPrefix)
  namespace: toolhive-system
spec:
  image: ghcr.io/stackloklabs/yardstick/yardstick-server:0.0.2
  transport: streamable-http
  proxyPort: 8080
  mcpPort: 8080
  serviceAccount: (join('-', [$testPrefix, 'mcpserver-sa']))
