apiVersion: v1
kind: Service
metadata:
  name: mcp-yardstick
  namespace: test-namespace
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 1800
