apiVersion: v1
kind: Pod
metadata:
  namespace: toolhive-system
  labels:
    toolhive.stacklok.dev/mcpserver: kagent-test
status:
  phase: Running
  (conditions[?type == 'Ready'] | [0].status): "True"