# Review and Update Printer Columns on Existing CRDs

**Parent**: [STORY-06](STORY-06.md) — Printer Columns

## Description

Audit all existing CRDs for missing or inconsistent printer columns and ensure a consistent experience across all `kubectl get` outputs.

## Implementation Notes

**Current state**:

| CRD | Columns | File:Lines |
|---|---|---|
| MCPExternalAuthConfig | Type, Age | mcpexternalauthconfig:684-685 |
| MCPServer | Status, Ready, URL, Age | mcpserver:799-802 |
| MCPRegistry | Phase, Sync, API, Servers, LastSync, Age | mcpregistry:674-679 |
| MCPRemoteProxy | Phase, Remote URL, URL, Age | mcpremoteproxy:290-293 |
| VirtualMCPServer | Phase, URL, Backends, Age, Ready | virtualmcpserver:320-324 |
| EmbeddingServer | Status, Model, Ready, URL, Age | embeddingserver:206-210 |
| MCPGroup | Ready | mcpgroup:78 |
| MCPToolConfig | ? | Check |
| VirtualMCPCompositeToolDefinition | ? | Check |

**Review checklist**:
- All CRDs should have `Age` column
- All workload CRDs should show Phase/Status
- Config CRDs should show Ready condition
- Any CRD missing useful columns should be updated
- MCPRegistry columns may need updating after STORY-05 (status consolidation)

**Post-implementation**: Run `task gen`.

## Acceptance Criteria

- [ ] All CRDs audited for printer column coverage
- [ ] Missing columns added where useful
- [ ] Consistent patterns across CRD categories (workload, config, consumer, org)
- [ ] MCPRegistry columns consistent with conditions-based status (if STORY-05 has landed)
- [ ] `task gen` succeeds
