apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ($testPrefix)
  namespace: toolhive-system
spec:
  template:
    spec:
      # Verify the custom ServiceAccount is used
      serviceAccountName: (join('-', [$testPrefix, 'mcpserver-sa']))
status:
  availableReplicas: 1
