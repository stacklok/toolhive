apiVersion: toolhive.stacklok.dev/v1alpha1
kind: MCPServer
metadata:
  name: backend-1
  namespace: toolhive-system
  labels:
    app: mcp-backend-1
spec:
  image: ghcr.io/stacklok/toolhive/mcp-backend-1:latest
  transport: stdio
  groupRef: test-group-controller
  env:
    - name: DEBUG
      value: "true"
