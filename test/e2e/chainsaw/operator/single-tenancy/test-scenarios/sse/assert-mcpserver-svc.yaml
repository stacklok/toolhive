apiVersion: v1
kind: Service
metadata:
  name: mcp-st-sse
  namespace: toolhive-system
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 1800
